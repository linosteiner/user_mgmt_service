# Internal address of the frontend: `auth-portal:3000`. The Ingress sends everything
# that is not /api here.
apiVersion: v1
kind: Service
metadata:
  name: auth-portal
spec:
  type: ClusterIP
  selector:
    app: auth-portal
  ports:
    - name: http
      port: 3000
      targetPort: 3000
