# The postgres image reads POSTGRES_DB on first start and creates that database. The
# backend reads the same key to build its JDBC URL, so the name is defined once.
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
data:
  POSTGRES_DB: user_mgmt_db
